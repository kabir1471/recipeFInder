import {
  SafeAreaProvider,
} from 'react-native-safe-area-context';

function App() {
  // const isDarkMode = useColorScheme() === 'dark';

  return (
    <SafeAreaProvider>
    </SafeAreaProvider>
  );
}

export default App;
